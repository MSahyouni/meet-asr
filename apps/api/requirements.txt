# ==== FastAPI Server ====
fastapi>=0.117.1,<0.118
uvicorn>=0.37.0,<0.38
starlette==0.48.0
python-multipart==0.0.20
aiofiles==24.1.0
requests==2.32.3
httpx==0.27.2

# ==== ASR (Whisper) ====
faster-whisper==1.2.0

# ==== Diarization (Pyannote & SpeechBrain for Enrollment) ====
pyannote.audio==3.3.2
speechbrain==1.0.0
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchaudio==2.5.1
pytorch-lightning==2.4.0 # Dependency for Pyannote

# ==== Summarization & NLP (Transformers) ====
# Jais-2 needs transformers>=5 + tokenizers>=0.20 (tokenizer.json merges format)
sentencepiece==0.2.1
transformers>=5.14.0,<6
tokenizers>=0.22.0
# transformers 5.x requires huggingface-hub>=1.5
huggingface-hub>=1.5.0,<2.0
# transformers 5.x needs accelerate>=1.1 for device_map
accelerate>=1.1.0,<2
protobuf==3.20.3
safetensors>=0.4.3,<1.0
# 2.7.x pins transformers<5 — use a release that accepts transformers 5
sentence-transformers>=3.4.0,<6
faiss-cpu==1.8.0
scikit-learn>=1.2,<1.6
joblib==1.4.2

# ==== Audio & Numerics ====
soundfile==0.13.1
resampy==0.4.3
noisereduce==3.0.3
librosa==0.11.0
# NumPy 2.x breaks torch, faiss, numba — keep 1.x
numpy>=1.24.4,<2
# Python 3.12 support requires a newer numba/llvmlite pair
numba==0.60.0
llvmlite==0.43.0
scipy>=1.11.4,<1.14

# ==== Optional TTS (Habibi) — see apps/api/requirements-optional.txt ====
#   ./.venv/bin/pip install -r apps/api/requirements-optional.txt

# ==== Utilities ====
PyYAML==6.0.3
python-dotenv==1.0.1
slowapi==0.1.9
limits==3.13.0
email-validator==2.1.1
bcrypt==4.2.1

# ==== Dev / Test ====
pytest>=8.0,<9